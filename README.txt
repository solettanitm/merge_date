I made this program in OS X. Before you run it, make sure data_events.sqlite and merge_date.sh are in the same directory.

How to run it
Open your terminal and navigate to the directory which contains these 2 files, and run . merge_date.sh, and follow the instruction to input.